services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: cofr-postgres
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    networks:
      - cofr-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # Golang Telegram Bot
  tg-bot:
    build: ../apps/tg-bot
    container_name: cofr-tg-bot
    restart: unless-stopped
    env_file:
      - ../apps/tg-bot/.env
    networks:
      - cofr-network
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: [ "CMD", "pgrep", "-f", "main" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  # FastAPI Backend
  server:
    build: ../apps/server
    container_name: cofr-server
    restart: unless-stopped
    env_file:
      - ../apps/server/.env
    ports:
      - "5784:5784"
    # Volume mounts removed for production - uses built image
    # For local development with hot-reload, use: docker compose -f infra/docker-compose.yml -f infra/docker-compose.dev.yml up
    networks:
      - cofr-network
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: [ "CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5784/health')" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # React Router Frontend (SSR)
  client:
    build: ../apps/client
    container_name: cofr-client
    restart: unless-stopped
    env_file:
      - ../apps/client/.env
    ports:
      - "3000:3000"
    # Volume mounts removed for production - uses built image
    # For local development with hot-reload, use: docker compose -f infra/docker-compose.yml -f infra/docker-compose.dev.yml up
    networks:
      - cofr-network
    depends_on:
      server:
        condition: service_healthy

  # Caddy Reverse Proxy
  caddy:
    image: caddy:2-alpine
    container_name: caddy-proxy
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    restart: unless-stopped
    ports:
      - "8080:80"
      - "8443:443"
    networks:
      - cofr-network
    depends_on:
      - server
      - client

  # Cloudflare Tunnel
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflare-tunnel
    command: tunnel run
    restart: unless-stopped
    volumes:
      - ./cloudflared:/etc/cloudflared:ro
    environment:
      - TUNNEL_METRICS=0.0.0.0:9126
    networks:
      - cofr-network
    depends_on:
      - caddy

networks:
  cofr-network:
    driver: bridge

volumes:
  postgres_data:
  caddy_data:
  caddy_config:
